<%= form_for @attendee, url: event_attendee_path(@event, @attendee),:html => { :method => :patch } do |f| %>

  <%= f.label :name, "Name" %>
  <%= f.text_field :name %>

  <%= f.submit "update" %>
<% end %>